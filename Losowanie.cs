using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Losowanie : MonoBehaviour
{



 /* 
   int rnd = Random.Range(1, 3);
        switch (rnd)
        {
            case 1:
                gold.gold = 10;
                break;
            case 2:
                gold.gold = 20;
                break;
            case 3:
                gold.gold = 30;
                break;
        }

    Procentowe losowanie:

    int rnd = Random.Range(1, 100);
    if (rnd < 30)
    {
    a + 1
    }


        */
}
